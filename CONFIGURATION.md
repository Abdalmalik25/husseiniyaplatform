# إعدادات تشغيل ALHUSAINIA

تعتمد المنصة على قيم افتراضية آمنة داخل `shared/config.ts`، ويمكن تجاوز قيم الواجهة عبر متغيرات Vite، وقيمة منح الرصيد المجاني عبر متغير الخادم. لا تُحفظ مفاتيح الدفع أو الأسرار داخل ملفات الواجهة أو المستودع.

## الهوية والاتصال

| المتغير | الاستخدام | القيمة الافتراضية |
|---|---|---|
| `VITE_BRAND_ARABIC_NAME` | الاسم العربي الظاهر | الحسينية |
| `VITE_BRAND_COMMERCIAL_NAME` | الاسم التجاري المختصر | ALHUSAINIA |
| `VITE_BRAND_LEGAL_NAME` | الصفة القانونية/التجارية | مؤسسة الحسينية لخدمات الأعمال |
| `VITE_BRAND_ENGLISH_NAME` | الاسم الإنجليزي الكامل | ALHUSAINIA Business Services Establishment |
| `VITE_BRAND_TAGLINE` | الوعد التسويقي الرئيسي | شريكك المهني لخدمات الأعمال المتكاملة |
| `VITE_SUPPORT_EMAIL` | بريد الدعم | فارغ حتى يُضبط |
| `VITE_SUPPORT_PHONE` | هاتف التواصل | فارغ حتى يُضبط |
| `VITE_SUPPORT_WHATSAPP` | رقم واتساب أو رابط التواصل | فارغ حتى يُضبط |

## الأرصدة والباقات

| المتغير | الاستخدام | القيمة الافتراضية |
|---|---|---|
| `VITE_TRIAL_CREDITS` | عرض مقدار التجربة في الواجهة | 3 |
| `TRIAL_CREDITS` | مقدار المنحة الفعلية في الخادم | 3 |
| `VITE_CREDIT_UNIT_LABEL` | اسم وحدة الرصيد | رصيد |
| `VITE_CREDIT_CONSUMPTION_LABEL` | وصف ما يستهلك الرصيد | طلب خدمة أو خطوة استشارية |
| `VITE_CREDIT_PLANS_JSON` | مصفوفة JSON لاستبدال الباقات الافتراضية | الباقات المعرفة في `shared/config.ts` |
| `VITE_CREDIT_CONSUMPTION_PRIORITY` | أولوية الاستهلاك في الواجهة | `free-first` أو `paid-first` |
| `VITE_CREDIT_COST_PER_ACTION` | تكلفة الإجراء المعروضة | 1 |

## الدفع القابل للتبديل

| المتغير | الاستخدام | القيم الممكنة |
|---|---|---|
| `VITE_PAYMENT_MODE` | وضع الدفع المعروض في الواجهة | `disabled` أو `manual` أو `stripe` |
| `VITE_PAYMENT_PROVIDER_LABEL` | اسم مزود الدفع الظاهر | Stripe |
| `VITE_CHECKOUT_ENABLED` | إظهار أن Checkout متاح | `true` أو `false` |
| `VITE_PAYMENT_SUCCESS_URL` | مسار العودة بعد نجاح الدفع | `/credits?payment=success` |
| `VITE_PAYMENT_CANCEL_URL` | مسار العودة بعد إلغاء الدفع | `/credits?payment=cancelled` |
| `PAYMENT_MODE` | وضع موصل الدفع على الخادم | `disabled` أو `manual` أو `stripe` |
| `PAYMENT_PROVIDER_LABEL` | اسم المزوّد في الرسائل الخادمية | Stripe |
| `PAYMENT_SUCCESS_URL` | مسار العودة الخادمي بعد النجاح | `/credits?payment=success` |
| `PAYMENT_CANCEL_URL` | مسار العودة الخادمي بعد الإلغاء | `/credits?payment=cancelled` |
| `STRIPE_SECRET_KEY` | مفتاح Stripe السري عند تفعيل الوضع الحقيقي | يُضاف سرياً من إعدادات المشروع |

## النظام التجاري والاشتراكات

| المتغير | الاستخدام | القيمة الافتراضية |
|---|---|---|
| `DATABASE_URL` | اتصال قاعدة البيانات (MySQL) | فارغ حتى يُضبط |
| `OAUTH_SERVER_URL` | بوابة المصادقة | يُضبط من إعدادات المشروع |
| `APP_ID` | معرف التطبيق في بوابة OAuth | يُضبط من إعدادات المشروع |
| `COOKIE_SECRET` | سر توقيع الجلسات | يُضبط من إعدادات المشروع |

### جداول النظام التجاري

- **subscriptions**: إدارة اشتراك المستخدم (trial/active/cancelled/expired) مع خطة الاشتراك وتاريخ الانتهاء والتجديد التلقائي.
- **devices**: تسجيل أجهزة المستخدم (web / desktop / mobile / cloud) مع آخر وقت مزامنة.
- **syncQueue**: طابور المزامنة للعمل دون اتصال — يسجل العمليات (create/update/delete) على الكيانات ويتم مزامنتها سحابياً.

### مسارات النظام التجاري

| المسار | الوصف |
|---|---|
| `/account` | حساب المستخدم: الرصيد، الاشتراك، الأجهزة المتزامنة، سجل الحركات |
| `/clients` | بوابة العملاء: خدمات مكتبية، هندسية، صيانة، تقنية، طلابية |
| `/credits` | الأرصدة والباقات ومسار الترقية |
| `/auth` | صفحة تسجيل الدخول |

### إجراءات tRPC الجديدة

| الإجراء | الوصف |
|---|---|
| `subscription.me` | جلب اشتراك المستخدم الحالي |
| `subscription.update` | تحديث خطة الاشتراك وحالته |
| `sync.registerDevice` | تسجيل جهاز جديد للمزامنة |
| `sync.devices` | قائمة أجهزة المستخدم |
| `sync.pending` | العمليات المعلقة في طابور المزامنة |
| `sync.enqueue` | إضافة عملية إلى طابور المزامنة |
| `sync.complete` | وضع علامة اكتمال على عملية مزامنة |

## مسار الإطلاق

للتجربة الداخلية، يمكن تشغيل المنصة بوضع الدفع المعطل مع منح مجانية. للمرحلة التجارية، تُضبط متغيرات الهوية والتواصل، ثم تُربط مفاتيح الدفع من إعدادات المشروع، وتُختبر معاملات النجاح والإلغاء والفشل والـ webhook قبل فتح Checkout للمستخدمين.

### متطلبات التشغيل الكامل

1. **قاعدة بيانات**: ضبط `DATABASE_URL` وتشغيل `npm run db:push` لإنشاء الجداول.
2. **بوابة OAuth**: ضبط `OAUTH_SERVER_URL` و`APP_ID` و`COOKIE_SECRET`.
3. **الدفع**: ضبط مفاتيح Stripe عند الجاهزية.
4. **PWA**: المنصة جاهزة للتثبيت كتطبيق سطح مكتب أو جوال من المتصفح.