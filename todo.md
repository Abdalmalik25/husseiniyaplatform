# Project TODO - نظام الحسينية المحاسبي المتقدم

- [x] إعداد هيكل جدول الميزانية في `drizzle/schema.ts` وتنفيذ الترحيل
- [x] بناء لوحة المتغيرات العلوية القابلة للطي وحفظها الدائم وتحديثاتها
- [x] تطوير نظام الاقتراحات الذكي والتوجيه المالي لمؤسسة الحسينية
- [x] تطوير جدول البيانات وداتاجريد مع الفلترة والبحث وحالة العمليات
- [x] توفير خيارات التصدير (CSV/Excel) والطباعة والمشاركة عبر واتساب
- [x] بناء لوحة القيادة (Dashboard) بملخصات الأرصدة والعمليات الأخيرة
- [x] كتابة واجتياز اختبارات Vitest لضمان سلامة العمليات ومسارات الخادم
- [x] المتجر الإلكتروني العام `/store` (كتالوج، سلة، إتمام طلب عام بلا تسجيل)
- [x] روتر `store` في الخادم: `catalog` + `placeOrder` مع خصم المخزون وإنشاء العميل تلقائياً
- [x] تحسين الواجهة التجارية: أسماء العملاء/الموردين في صفوف الفواتير، الوسم «متجر إلكتروني» للطلبات الواردة من الموقع، حوار التعديل وضبط المخزون للمنتجات
- [x] أدوات الدمج مع موقع الويب: `client/public/Store.php` (iframe) و `SyncJav.js` (bridge)

- [x] إقفال الدورة المحاسبية (معاينة أرصدة + قيود إقفال غير قابلة للتكرار إلى حساب النتائج) في تبويب الدليل
- [x] استيراد/تصدير CSV للأصناف والخدمات في تبويب المنتجات (رؤوس عربية/إنجليزية، تحديث بالرمز)
- [x] تحسين الأداء: staleTime + refetchOnWindowFocus=false + debounce لبحث المتجر
- [x] إضافة الصفحة التعريفية التفاعلية `/about` لمؤسسة الحسينية لخدمات الأعمال (استشارات هندسية، تقنية، مؤسسية، وإدارية) ومكتبة الحسينية الحديثة (خدمات طلابية ومكتبية، تصاميم، بحوث، وصيانة الأجهزة)
- [x] تطوير هيدر وقائمة ملاحة موحدة `HeaderNavbar` لربط جميع صفحات المنصة بسلاسة
- [x] إضافة زر ونموذج طلب الخدمات والاستشارات المباشرة مع توليد كود الطلب والربط بالواتساب
- [x] إضافة البذور التلقائية idempodent لكتالوج الخدمات والأصناف لمؤسسة ومكتبة الحسينية
- [x] بناء وتشغيل وتوفير رابط المنصة النهائي المعتمد للتثبيت والتصفح: `https://alhusainiaye.vercel.app`
- [x] إعادة هيكلة حسابات التقارير المحاسبية (ميزان المراجعة، قائمة الدخل، الميزانية العمومية) في دوال نقية قابلة للاختبار `client/src/lib/accountingReports.ts` مع 6 اختبارات Vitest تضمن توازن القيود المزدوجة والهويات المحاسبية
- [x] سداد ديون الجاهزية الإنتاجية: إزالة `client/public/manifest.json` المكرّر غير المستخدم + توحيد رؤوس كاش `manifest.webmanifest` في `vercel.json` + أولوية جلب عالية لصورة المساعد + إعادة تزامن تلقائية عند عودة اتصال الأوفلاين (`refetchOnReconnect`)
- [x] توحيد اسم النظام إلى `Uamex_erp` عبر كل شاشات العمل والوثائق والمرجعيات، واعتماد الشعار الرسمي (`uamex-erp.png` / `uamex-favicon-32.png`) في شاشة البداية والبوابة والتذييل وهوية الهبوط مع favicon خاصة — مع إبقاء المعرفات الداخلية للمرونة المستقبلية
- [x] سداد الديون التقنية: هوامش أمان المصادقة (rate-limit/قفل الحساب) + إصلاح cache-busting لقاعدة البيانات الأوفلاين (Dexie) + بوابة جلسة صفرية الثقة لمسارات `query.*` (`UNAUTHORIZED` للمجهولين)
- [x] E2E كامل 16/16 (Playwright): بوابات الحماية غير المصدّق عليها، رحلة تسجيل الدخول ← لوحة القيادة ← كل مساحات العمل، والبحث الشامل عبر لوحة الأوامر Ctrl+K بنتائج معزولة بالمستأجر — صفر تخطٍّ (0 skipped) و`tsc --noEmit` نظيف
- [x] اختبار الحمل k6 (الإصدار v2.2.0 مثبّت محلياً): `scripts/load-test.js` (سيناريوهات smoke/quick/load/stress + أدوات `scripts/run-load-test.ps1` و`scripts/dl-k6.mjs`) — يجتاز smoke 3 VUs/30s بـ 100% فحوص ناجحة وعتبات نظيفة (errors=0%)؛ النقاط المختبرة حقيقية: `auth.me`, `query.dashboardSummary`, `erp.listDepartments`, `financialReports.trialBalance` + واجهتا المتجر `catalog` و`orders/track`، مع المرور بوابات tRPC الفردية (GET+input المرمّز) واعتماد Bearer fallback للمصادقة، وإدراك سلوك rate-limit (429) المتوقَّع على المسارات العامة فقط

- [x] سداد دين الترحيلات الوظيفية: إعادة كتابة `drizzle/0011_enterprise_scale_optimization.sql` على المخطط الفعلي (تم التحقق منه عبر information_schema) بدل المخطط الوهمي snake_case — فهارس مركّبة/جزئية/تغطية على `journal_entries`+`transactions` و`sales/purchase_invoices` (paidAmount) و`stock_movements`+`warehouse_stock` و`audit_logs`، تقريران مجمّعان `mv_monthly_pl` + `mv_customer_aging` مع فهرس فريد ودالة `refresh_enterprise_views()`، فهرس trgm للبحث النصي، قيد منع المخزون السالب، وضبط autovacuum/STATISTICS للجداول عالية الكتابة — مُطبَّق ومُتحقق منه (12/12 ترحيلات، failures=0)
- [x] سداد ديون تقنية في الخادم: إصلاحات TypeScript الصارمة في `auditLogger.ts` (نوع `NeonQueryFunction<false,false>` الصحيح، تمرير `fullEntry` الكامل للتنبيهات، typing دقيق للأجgregateات) + إعادة هيكلة مُقسّم جُمَل SQL في `migrate.ts` مع اختبارات `migrate.test.ts` المحدثة — 124 اختبار Vitest ناجح، `tsc --noEmit` نظيف، ESLint صفر أخطاء
- [x] نشر الإصدار للإنتاج: بناء ناجح (Vite + serverless bundle `api/agent.mjs`) ونشر Vercel للإنتاج مع الربط بالدومين `https://alhusainiaye.vercel.app` — فحوص ما بعد النشر: `/api/health`=200، الصفحة الرئيسية=200، النقاط المحمية تعيد 401 لغير المصادقين

- [x] **إكمال دورة استعادة الحساب من طرف إلى طرف (Auth Cycle)**: صفحتا `/reset-password` و `/verify-email` بروح تصميم الحسينية (RTL + ink/brand) مع تحقق تلقائي وإعادة إرسال ذكية، وتعريض `auth.verifyEmail` و `auth.resendVerificationEmail` على روتر التطبيق الرئيسي (كانا محبوسين في `authRouter.ts` غير المُستخدم) — الرمز use-once يلغى فور استخدامه (server-side)
- [x] **إصلاح نطاق رسائل البريد**: روابط التحقق/الاستعادة كانت تُبنى من `OAUTH_SERVER_URL` (بوابة OAuth) → أصبحت من `APP_URL` (نطاق المنصة) مع fallback إلى `https://alhusainiaye.vercel.app`
- [x] **تحصين محمّل i18n** (إزالة import attribute من `ar.json/en.json`) + رفع الإصدار إلى **2.14.0** (brand + package + CHANGELOG موحّد)
- [x] **ملاحظات الأداء**: نقطة `/api/performance` + `performanceMiddleware` (X-Request-ID) + `healthCache` لفحص قاعدة البيانات بذاكرة 5 ثوانٍ في `server/_core/app.ts`
- [x] **الإصدار الإنتاجي النهائي 2.23.0 — سدّ الديون ورفع UX عالمياً End-to-End (2026-09-06)**: إصلاح XSS في `pdfInvoiceGenerator` (`escapeHtml` شامل) + إصلاح `financialReportsRouter` الحاجب (`accountId` + `previousBalance`) + تنسيق 42 ملفًا — `pnpm check` EXIT 0 / `pnpm lint` 0 أخطاء / `pnpm test` 157/157 / `pnpm build` EXIT 0 (23.2s، api/index.mjs 1.3mb)؛ نظام ألوان دلالي 6 ثيمات (`--success/--warning/--info`) + `ModuleIdentity.pitch/kpi` + `cycleCountLines` + تقارير مقارنة `previousAsOf` + طباعة ضريبية آمنة؛ انتقال كامل `Home/Billing/POS/Commercial/Inventory` إلى `panel-premium/datagrid/chip/empty-state/ribbon-premium/hover-lift/press-effect` مع WCAG AA + reduced-motion + RTL — جاهز للنشر على `https://alhusainiaye.vercel.app`
- [x] **حملة الترقية العميقة 2.24.0 — الشريط والتقارير والذكاء (2026-09-06)**: إصلاح عشوائية الشريط (`lib/nav.ts`: مجالات `PLATFORM/INTELLIGENCE/TOOLS` + `HeaderNavbar.tsx`: 3 عناقيد ميغا دقيقة بـ prefetch كامل لـ 7 مسارات BI)؛ إعادة هندسة `Reports.tsx` من حوسبة عميل إلى مصدر خادم موثوق `financialReports.*` مع `asOf` + `tabs-primary/datagrid/panel-premium/empty-state` + تصدير/طباعة؛ ترقية `FinancialStatements.tsx` بمقارنة فترات `previousAsOf` و`chip/status-strip` لكل aging؛ تصحيح `Analytics.tsx` (fy من Oct-Mar الخاطئ إلى سنة تقويمية واضحة + ألوان `var(--*)` + دلتا شهرية) — `check/lint/test/build` صفر أخطاء — منشور على `https://alhusainiaye.vercel.app` v2.24.0
- [x] **الفصل العالمي 2.25.0 — الموقع التعريفي vs النظام (2026-09-06)**: فصل جذري `lib/nav.ts:315` `APP_GROUPS` خمسة مجالات تشغيلية (مالية/تجارة/تشغيل/ذكاء/حوكمة) + `HeaderNavbar.tsx:122` شريط ذكي `isMarketingShell` يبدل `bg-sand/90` مع شارة "الموقع التعريفي" إلى `bg-ink-deep/90` مع "نظام التشغيل" + إخفاء عنقود الذكاء عن الزائر؛ `AppSidebar.tsx:415` من قائمة مسطحة 27 بند إلى 5 أقسام عنوانية مع فاصل ووصف؛ مظهر منفصل (موقع `bg-sand+glass` يبيع القيمة vs نظام `bg-background+panel/datagrid` يشغل القرار) وأدوات منفصلة (حاسبات/معرفة للموقع vs نقاط بيع/مخزون للنظام) — `check/lint` صفر — منشور v2.25.0
- [x] **العصرية القصوى 3.0.0 — Heritage Futurism (2026-09-06)**: `index.css` v3.0 (fluid hero/clamp + shadow-modern + glass-ultra 24px/saturate 1.4 + bento-grid + btn-pill + aurora-mesh + stagger) — `Landing` هيرو سائل + Bento زجاجي + `PricingTeaser` Bento حبوبي + `BudgetsPanel` خالٍ من slate — `docs/DESIGN_SYSTEM.md` v3.0 — `check/lint/format` صفر — منشور v3.0.0
- [x] **القمة 4.0.0 — Apex Luxury (2026-09-06)**: أقوى (حافة `s-maxage=10` + `COEP/X-Robots/Server-Timing/Cache-Tag` في `vercel.json:53`) + أرقى (`index.css:3201` نسيج حريري SVG + تدرجات OKLCH سينمائية + ظلال فاخرة + `bento-asymmetric` غير متماثل + حركة ربيعية + `glass-silk`) — `Landing` هيرو سينمائي + `HeaderNavbar` زجاج حريري + `AppSidebar` حريري — منشور v4.0.0
- [x] **الخبراء 4.1.0 — سداد الديون الأربعة (2026-09-06)**: تقني (`_journal` 0010-0013 + إزالة `public s-maxage` + منع silent skip + تحويل slate→توكنات) + تشغيلي (Cron موزع + Health صادق + تسامح checksum) + وظيفي (منع `tenantId=0` في voucher + حماية حذف مركز تكلفة + ربط `ClaimSubscription` بالجلسة) + تصميمي (تباين AA + Bento) — `check/lint/format/test` صفر — منشور v4.1.0
- [x] **المحتوى الخبير 4.2.0 — إعادة هندسة تسويقية (2026-09-06)**: ترتيب `Cost→Methodology` و `HowItWorks` بعد `Uamex` بقصة `Problem→Solution→Proof→Offer` + رفع شريط التنبيه من سطحي إلى `IFRS/COSO/PMBOK` + هيرو `مصدر حقيقة/أمان/أثر` كمّي + فرض 8 أيقونات رسمية + `tools-lab` Bento 4 أدوات + حاسبة BOQ مصغرة تفاعلية — `check/lint` صفر — منشور v4.2.0
- [x] **الخبير العالمي 4.3.0 — إلغاء الكود الميت وفرض التحديث (2026-09-06)**: حذف 10 مكونات ميتة (`AnimatedCounter`/إلخ) + `workspaces` + `NAV_ITEMS` + تنظيف `template.json` TODO — فرض التحديث الأخير + جودة محتوى بلا سطحية (كل ادعاء بمعيار/رقم) + أيقونات رسمية + أدوات في مكانها الصحيح — `check/lint/format` صفر — منشور v4.3.0
- [x] **المعلومة 4.4.0 — تسويق بلا أرقام ولا شهادات (2026-09-06)**: إزالة الأرقام الاستعراضية (`stats` `1,200/99.6%` → `قراءة/تتبع`، `industry` `-35%` → `وضوح`، `testimonials` `14 يوم` → `سياق`، `Landing` `500+`/`60 ثانية`/`48 ساعة`/`±5%`/`ISO` → `بمعيار/بمعلومة/حوكمة`) وإزالة الشهادات (`ISO/GDPR` → `معلومة/قيمة/سياق`) — تسويق بالمعلومة والقيمة الحقيقية بلا رسائل مستهلكة — `check/lint` صفر — منشور v4.4.0
- [x] **البوابة 4.5.0 — تعميم نظام التصميم بذكاء (2026-09-06)**: `enforce-design-system.mjs` يفحص 17 شاشة عمل ويمنع `pnpm build` إذا وجد `bg-slate` — ترقية `Commercial 110→0` و `Inventory` و `Requisitions` و 4 شاشات أخرى إلى `panel-premium/shadow-modern/bg-success` دلالي — `prebuild` يفرض البوابة — `check/lint` صفر — منشور v4.5.0 (يمنع النشر إلا بعده)
- [x] **النووي 5.0.0 — الوحدات التشغيلية بقوة نووية End-to-End (2026-09-06)**: مالية (`accountingEngine` فحص `isActive` + `baseAmount` تلقائي + `assertPeriodOpen`) + تجارة ومخزون (`Commercial/Inventory/Requisitions` Bento + دلالي) + تشغيل وموارد (`Projects/HR`) + ذكاء وحوكمة (`Reports/Analytics` مصدر حقيقة واحد) — `enforce-design-system` ✅ · `check/lint` 0 — منشور v5.0.0
- [x] **الفصل الحقيقي 5.1.0 — شركة vs منتج بلا خلط (2026-09-06)**: `MarketingHeader` أبيض `الرئيسية→من نحن→قطاعات الأعمال→المزيد` بلا `v{version}` ولا `AES` + `AppHeader` داكن 48px منفصل + `MarketingLayout/AppLayout` في `App.tsx:121` + `About` هرم `رسالة قوية→رؤية/رسالة→حلول` — `check/lint/build` 0 — منشور v5.1.0
- [x] **النهائي 6.0.0 — الإصدار النهائي بلا سطحية (2026-09-06)**: هرم `Company→Solutions→Uamex كمنتج مفصول→Sectors→Trust→Pricing` + إزالة `قيد مزدوج/IFRS/COSO/Big Four/AES` من الواجهة العامة إلى لغة تاجر `وضوح مالي يومي/حماية متقدمة` + تسعير مستوحى من **دفترة** (6 تطبيقات بسعر واحد + أول مشمول) و **أودو** (One App Free) — `check/lint/test 157/build` 0 — جاهز للنشر 6.0.0
